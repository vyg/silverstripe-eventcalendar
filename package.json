{
  "name": "eventcalendar",
  "version": "1.0.0",
  "description": "Silverstripe Event Calendar",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:js && npm run build:css",
    "build:js": "browserify src/js/index.js > dist/js/eventcalendar.js",
    "build:css": "node-sass --include-path ./src/scss --include-path ./node_modules src/scss -o dist/css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "silverstripe",
    "event",
    "calendar",
    "fullcalendar"
  ],
  "author": "Voyage",
  "license": "ISC",
  "dependencies": {
    "fullcalendar": "^3.4.0"
  },
  "devDependencies": {
    "browserify": "^14.4.0",
    "node-sass": "^4.5.3",
    "npm-css": "^0.2.3"
  }
}
